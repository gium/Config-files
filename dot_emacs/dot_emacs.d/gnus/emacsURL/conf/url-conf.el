
;; la vielle feinte autour du load-path.
;;
(setq load-path                                                              
      (push                                                                  
       "/u/guest/dp/public/emacsURL/emacs/site-lisp/"
       load-path))

(require 'url)
